version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: akinel-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-akinel_db}
      POSTGRES_USER: ${POSTGRES_USER:-akinel_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-akinel_pass}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-akinel_user} -d ${POSTGRES_DB:-akinel_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
